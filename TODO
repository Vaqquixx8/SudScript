TODO

Language Features
-----------------
[x] Binary Expressions
[x] Unary Expressions
[x] Variables
[x] Functions
[x] If / Else / Else If
[x] Logical AndAnd and OrOr
[x] Lists
[ ] Member Access / Structs
[x] Allow Sud Projects
[ ] Control Flow Improvements

Lists
-----
[x] Tokenize
[x] Parse
[x] Runtime Object
[x] Functions
    [x] :get(index) -> any
    [x] :set(index, thing) -> void
    [x] :add(thing) -> void
    [x] :remove(thing) -> void
    [x] :size() -> number
    [x] :fill(thing) -> void
    [x] :clear() -> void
    [x] :contains(thing) -> bool
    [x] :first(thing) -> number

Member Access / Structs
-----------------------
[x] Structs
    [x] Fields
    [x] Methods
        [x] Instance
        [x] Shared

[ ] Modules (Structs)
    [ ] need
    	[x] Import Groups
    	[ ] Qualified Name Access ------------------------------ Why the fuck is this so hard!?
    		[ ] alias (need <import/group name> as <alias>)
    [x] Re-Factor
        [x] groups (group <group name>)
            [x] File-Name independant searching (Find by group name declaration)
            [x] SubGroups (group myGroup:mySubGroup)
                [x] Import all subgroups below highest level group declaration
                (with 'a:b:c' - 'need a' imports 'a', 'a:b', 'a:b:c')
                (with 'a:b:c' - 'need a:b' imports 'a:b', 'a:b:c')
                [x] Allow Circular need statements

[x] Standard Library Modules (Separate from built-in functions, eg. list[] :add(), :remove() etc.)
    [x] Add Sud:IO
    [x] Add Sud:Math
    	[x] Move all current Math Functions into Sud:Math

Allow Sud Projects
------------------
[x] SudManifest
    [x] ROOTPATH/Sud.SudManifest
        [x] Project Name
        [x] Paths
            [x] Project Relative Paths
                [x] /src
                [x] /src/Main.sud
                [x] /src/Modules/

Control Flow Improvements
-------------------------
[ ] Named loops
    [ ] Loop identifiers
    [ ] break <loopName>
    [ ] continue <loopName>
[ ] Misc
	[x] /=/*=/+=/-= operator
	[x] ++/-- operator
	[ ] Block Comments
		/* This
		is all a
		single
		comment */
[ ] 'switch' Statement (Name as 'jump')
	[ ] Lexer
	[ ] Parser
	[ ] Interpreter
[x] Empty variable declarations "let myVar = null"

Endgame Shit
------------
[x] Add project building
	[x] "sud build" CLI Command
		[x] Add the Command
		[x] Make the Command actually do something
[ ] Rewrite in Another Language
	[ ] Initial Rewrite
	[ ] New CLI
[ ] Bootstrap
	[ ] Rewrite Again
