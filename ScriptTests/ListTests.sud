let list = []
func main()
{
	// We don't have a null literal, once we do, maybe change it so
	// that "fill" is not responsible for list size initialization
	// and instead use something like "setSize(size, optionalValue? = null)" or "init(size, optionalValue? = null)"

    list:fill("", 10)

	list:fill("newThing")

	say(list:length())
}